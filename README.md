# Mi Alquiler (Laravel + Inertia + React + Tailwind)

Requisitos: PHP, Composer, Node, npm/yarn, DB.

### Setup
composer install
cp .env.example .env
php artisan key:generate
npm install
npm run dev
php artisan migrate
