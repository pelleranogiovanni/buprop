# Buprop (Laravel + Inertia + React + Tailwind)

Plataforma web para publicación y gestión de propiedades en alquiler y venta.

Requisitos: PHP, Composer, Node, npm/yarn, DB.

### Setup
composer install
cp .env.example .env
php artisan key:generate
npm install
npm run dev
php artisan migrate
